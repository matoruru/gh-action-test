name: CI

on:
  workflow_dispatch: {}
  create:
    branches-ignore:
    - '**'
    tags:
    - 'v*.*.*'
    - 'v*.*.*-rc*'
  pull_request:
    branches:
    - main
    types:
    - closed

jobs:
  build:
    runs-on: ubuntu-latest
    environment: build
    steps:
      - name: Say hi
        run: |
          echo hi
